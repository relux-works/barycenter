# TASK-260727-1msjz6: two-independent-barycenters-air-e2e

## Description
Run the real different-homes acceptance journey using the installed macOS and ssh-win applications: keep or create one independent Barycenter per machine, create an Air on Mac, transfer only the one-time Air invitation, join from Windows, and verify shared-Air behavior without pairing the devices into one identity.

## Scope
Use the exact installed Mac and Windows airfix candidates and the authoritative coordinator. Prove that the machines hold different Barycenter identifiers and credentials; never use Create device invitation. Exercise Mac Air creation and initial invite, secure one-time invite transfer, Windows review/confirm/join, saved membership and active-Air state on both sides, restart persistence, one observable shared playback/control path when the runtime permits, duplicate/transitive-delivery checks, and safe cleanup. Capture timestamps, hashes, redacted screenshots and logs. If the disconnected Windows console makes GUI interaction impossible, exhaust the existing approved UI harness and report one precise owner-only step instead of claiming PASS.

## Acceptance Criteria
Evidence shows two distinct Barycenters on different machines before the invitation and no credential or recovery-file sharing. Mac creates one Air and one single-use Air invitation; Windows reviews and consumes it exactly once and joins as a separate member. Both clients show the same Air membership and expected active/saved state after refresh and restart. At least one shared runtime action reaches exactly the intended two members without duplicate or transitive delivery, or the unavailable media prerequisite is recorded as a narrowly scoped blocked row while the identity and membership journey still has an honest verdict. Reusing the invite fails safely. No secret is written to logs or board resources, no device-pairing flow is used, and the final receipt gives PASS/FAIL/BLOCKED for every row.
