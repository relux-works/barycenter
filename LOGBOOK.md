# Flight Logbook

> Institutional memory. Concise, factual, high-signal.
> Newest entries first. One block per insight.

## 2026-07-30

### 1207 — BUG-260729-3kecpr unlocked-console validation and signed 958.4 smoke
- RESOLUTION: the physical console was unlocked without weakening file protection or adding a product/test workaround. The exact full Swift gate passed all 390 tests/62 suites, including the three `.completeFileProtection` outbox tests that correctly failed while the console was locked. The focused 66-test/8-suite gate, strict task-owned `swift-format`, scoped `git diff --check`, and optimized release build also exited 0.
- EVIDENCE: installed Developer-ID Pulsar 0.3.0 (958.4) retained NodeApp SHA-256 `dc022c30cd3f7840324232d73df6bf044471e5f2b5063f39c768aec4f8f5f137` and passed deep/strict codesign. With the built-in default microphone at 48 kHz and default speakers at 44.1 kHz, Record exposed the plain recording/Stop state with no alert, sheet, quality prompt, or route objection; Stop produced a mono 48 kHz Int16 21-second draft and left no partial.
- FINDING: a transient authenticated-app-data refresh error initially masked the draft card, but a clean relaunch restored the 2/2 coordinator view and displayed the same durable `Pulsar recording` card. Targeted recording-interval logs contained no `!dev`, `560227702`, DDAgg, VPIO, or voice-processing event. The developer-created draft was then removed through Pulsar's own `Delete local draft` action, leaving CaptureMedia empty.

## 2026-07-29

### 1839 — BUG-260729-3kecpr ordinary recording split from unstable default-duplex aggregate
- FINDING: production 0.3.0 (958.2) failed ordinary MacBook Pro clip recording with Core Audio `!dev` (`560227702`) after VPIO constructed a transient default input/output aggregate and device 429 disappeared. The physical default input remained healthy at 48 kHz while default output was 44.1 kHz; persisted microphone selection was the ephemeral numeric `AudioDeviceID` 92.
- DECISION: ordinary clips now use a microphone-only `AVAudioEngine` session surface that never accesses `outputNode`, voice processing, an output-route resolver, or the capture-quality configurator. The former strict VPIO implementation remains isolated as `MacAVAudioFullDuplexCaptureBackend` for actual simultaneous input/output workflows. Accountless cue/output graph construction is deferred until input capture requests the start cue, so an output failure becomes `cuePlaybackFailed` with partial cleanup instead of disabling Record.
- DECISION: microphone persistence moved to stable Core Audio UID key `captureInputDeviceUID.v2`; numeric v1 values are cleared, valid prerelease UID values migrate only while present, and stale selections fall back to the current default. A selected external device disappearing during attachment logs the typed startup diagnostic but surfaces the dedicated selected-device recovery.
- REVIEW: independent review first found eager accountless output startup and generic attachment-race copy; both were corrected with deterministic regressions. Independent re-review approved with no remaining code or acceptance findings.
- EVIDENCE: focused Swift gate 66 tests/8 suites exit 0; strict task-owned `swift-format`, scoped `git diff --check`, optimized release build, Developer-ID packaging, candidate/installed deep codesign, and installation all exit 0. Revised installed candidate is Pulsar 0.3.0 (958.4), Team ID `262RZ595FP`, NodeApp SHA-256 `dc022c30cd3f7840324232d73df6bf044471e5f2b5063f39c768aec4f8f5f137`. `spctl` is truthfully red exit 3 because local ASC notarization credentials are absent.
- BLOCKER: after the first successful 958.3 physical Record → Stop → mono 48 kHz draft smoke, the console locked (`IOConsoleLocked=true`). Revised full suite ran 390 tests and exited 1 solely on the three unrelated protected-outbox tests; a direct write matrix reproduced EPERM only for `.completeFileProtection`, which is intentionally unavailable while locked. Revised 958.4 physical smoke and full green rerun require a human console unlock; no protection weakening, temp override, product flag, or unlock workaround was added.

## 2026-07-28

### 0511 — BUG-260728-3mx5hm reviewer rework → AVAudioEngine release + executable consent coverage
- FINDING: review correctly identified that a failed `AVAudioEngine.prepare()` / `start()` attempt could leave `isRunning == false` while still retaining prepare-allocated VPIO/default-duplex resources. `reset()` resets nodes but does not replace the resource-release role of `stop()`.
- DECISION: every failed startup attempt now unconditionally calls `stop()`, removes the tap, resets the engine, and clears the mailbox before retry. A production-used startup sequencer permits one consented fallback only after that release; deterministic ordering tests prove release before the next prepare/start and before disabling VPIO.
- DECISION: the application composition now delegates one-generation prompt/cancel/retry/reset policy to `MacCaptureConsentCoordinator`. Executable reducer tests cover built-in-speaker rejection, accepted headphones, fail-closed cancel with no retry, exactly one consented retry, generation-close revocation with busy/deferred application, eligible initial VPIO fallback, and no re-prompt after a consented terminal fallback failure.
- EVIDENCE: focused Swift gate 52 tests/6 suites exit 0; full Swift gate 378 tests/60 suites exit 0; strict task-owned `swift-format`, scoped `git diff --check`, and optimized `NodeApp` release build all exit 0. Release build warnings remain the pre-existing `PlayerCore`/payload Sendable warnings outside this task.

### 0435 — BUG-260728-3mx5hm macOS recording quality/startup recovery → developer handoff
- FINDING: the retained production receipts isolate two fail-closed built-in-speaker paths: undiscoverable `capture_captureQualityUnsupported`, then a VPIO `CADefaultDeviceAggregate` race where HAL start returned CoreAudio status 35 and the aggregate became valid 11–23 ms later. Explicit input selection did not change the result, and neither path finalized a partial draft.
- DECISION: recording failures crossing into SwiftUI are typed and exhaustively mapped to localized English/Russian guidance. A native alert offers “Use headphones” or “Allow this limited recording”; cancel selects the fail-closed headphone path, while explicit consent retries the same primary recording action and is revoked when that capture-quality generation closes.
- DECISION: VPIO startup recovery is restricted to CoreAudio-domain status 35 or invalid/changing duplex layouts, at most four attempts in a 125 ms window with 25/35/45 ms delays. Only a terminal initial voice-processing attempt may enter the unprocessed fallback, and only with explicit degraded consent; input-selection errors, unrelated numeric code 35 errors, and failures from the consented fallback cannot reopen that path.
- EVIDENCE: deterministic tests cover the late-valid aggregate, repeated layout churn, typed/redacted diagnostics, unrelated code 35, consent/cancel/reset, accepted headphones, localized copy, and no retained draft after terminal startup failure. Focused Swift gate: 44 tests/5 suites, exit 0. Full Swift gate: 370 tests/59 suites, exit 0. Strict task-owned `swift-format` lint, `git diff --check`, and optimized `NodeApp` build all exited 0; the build retained pre-existing `PlayerCore`/payload sendability warnings outside this task.

### 0256 — TASK-260727-1msjz6 v4 resume → owner-reported Windows join did NOT commit; ROOT CAUSE = multi-instance WS reconnect storm
- CONTEXT: resume RUN-260727-b63b90 after owner did the full GUI sequence — Windows online, Mac created Air `hhome-test` + one member invite ("код есть"), invite scp'd to Windows Desktop, Windows Join→Review→Confirm ("вошел"). Ask: read-only verify one shared Air with orbit 9 + orbit 10 membership, active/saved, invite consumption.
- FINDING (first-hand, Mac authoritative Airs owner view, re-fetched via explicit "Refresh Airs" `AXButton`): `hhome-test` = **1/8 members**, 0 active, Room state **Parked**, role Owner → Windows (orbit 10) is **NOT a joined member**. Coordinator `member_count` counts only `status='joined'` (`coordinator/internal/store/air_control.go:340`); `pending_confirmation` is excluded → owner view cannot distinguish "never joined" vs "stuck pending". The AC-required 2-member shared Air **does not exist**. Evidence: `mac_air_membership_1of8_redacted.png`.
- ROOT CAUSE (new, first-hand, live 22:52Z, `windows_client_state_masked.txt`): Windows client in a **WS reconnect storm** — `connected → welcome → close 1006 (abnormal closure): unexpected EOF → reconnect` every ~2 s, plus `librespot exited exit status 1`. **THREE** concurrent `pulsar-win-amd64` processes running (PIDs 7200 [01:29], 11572 [23:44 orig onboarding], 13300 [00:17 v3 paired]) contend for the single orbit-10 identity; coordinator evicts duplicate node connections → node never holds a stable session → the Air Review→Confirm (`ConsumeAuthorizedAirInvite` → `ConfirmAuthorizedAirJoin`, both joiner-side, `currentPrimary`) cannot commit to `joined`. Same family as Anomaly A / BUG-260728-28mp7v, one layer up.
- FINDING (architecture, from two Explore passes): Air membership is **in-memory/GUI-only on BOTH clients** — no disk persistence, no os_log, no bearer-free local read surface (`AirAppClient`/`MacAirAppComposition.swift:237`; `pulsar-win/windows_air_composition.go:437`). Coordinator has no unauthenticated Air read (`GET /v1/airs` etc. all `withAirControl` bearer). So the only read-only membership observation is the Mac GUI (aggregate counts + own role; no per-member ids shown) and the Windows GUI (unreachable via ssh session-0). `MacAirAppComposition.loadProjection` is pure GET — refresh caused no mutation.
- DECISION: routed **blocked**. Not `done` (2-member Air absent; claiming PASS = dishonest). Not `to-review` (no autonomous rework advances it — Air Join is GUI-only on the Windows console, ssh session-0 ≠ desktop session-1, no Air UI harness; raw-API forgery refused). No product code changed → rows 10-11 unchecked (synthetic hardware-sim tests = forced fit).
- SECRET HYGIENE: 4 full-window Air screenshots transiently showed the live invite code → **deleted**; retained only a code-free membership-card crop. Windows invite-code file NOT deleted (owner instruction: keep until reuse-check reachable). Bundle secret-scanned clean (only token fingerprints + public git SHA + a test name matched).
- PRECISE OWNER STEP: on Windows console close ALL Pulsar instances (3 running), launch exactly ONE, confirm STABLE connect (no repeating close-1006) + `/healthz nodes_connected` steady 2, THEN Airs→paste code→Review→Confirm and verify the Mac card flips **1/8 → 2/8**; then restart-reverify, invite-reuse-must-fail, dissolve+cleanup.
- RECOMMEND: orchestrator file a client bug — no single-instance lock ⇒ duplicate orbit-10 WS connections ⇒ reconnect storm blocks Air join (likely BUG-260728-28mp7v family).
- Artifacts: `TASK-260727-1msjz6_receipt-v4.md`, `_evidence.v4.tgz`, `_mac_air_membership_1of8.png`.

### 0137 — TASK-260727-1msjz6 v3 resume → Anomaly A RESOLVED (Windows orbit 10 ONLINE); GUI create/join still blocked
- MILESTONE: after Ivan relaunched Windows Pulsar (owner step 1 of the v2 receipt), the Windows Barycenter (orbit 10) is now a connected coordinator node. Coordinator `/healthz` `nodes_connected` 1→**2**, `orbits`=10, `air_rooms_enabled`=true / `airs_authoritative`.
- FINDING (Anomaly A resolution, first-hand masked Windows log `windows_log_masked_v3.txt`): relaunch PID 13300 (started 2026-07-27T21:17:46Z) took the PAIRED path — `pulsar-win running … orbit=10 device_name="Pulsar A"` → `connected to coordinator wss://…/ws` → `welcome mode=shared`. Earlier onboarding starts (21:17/21:45/23:44 local) reached only the unpaired tray loop. Confirms both the anomaly AND that relaunch is the working remedy. Underlying first-launch onboarding→paired auto-transition defect is tracked as **BUG-260728-28mp7v** (`windows-onboarding-stays-unpaired-until-relaunch`, `backlog`).
- FINDING (Row 6 partial): Windows identity persistence across restart PROVEN first-hand — DPAPI credential survived the relaunch and the client returned as the SAME orbit 10, connected. `control.context=active`.
- MILESTONE (tester value-add): ran the pulsar-win Air unit tests on the REAL Windows host for the first time (prior runs only cross-compiled) — **14/14 Air tests PASS** (hermetic scripted mock doer; no coordinator/credential touch). One first-pass FAIL (`TestWindowsAirCompositionHasNoPhaseOneTargetOrInboxDependency`, `open windows_air_composition.go: cannot find file`) was an execution-env artifact of running the standalone binary outside its package dir; re-ran with the source present → PASS. Not a defect.
- FINDING: the working-tree Air changes add only a read-only `Availability()` capability probe (both clients, `/healthz`→`air_rooms_enabled`+`air_authority_state`) + onboarding fixes — NO headless Air create/invite/join lever exists on either client. New probe code already covered by dedicated tests (`TestAirClientReadsCoordinatorAvailability`, swift `"Coordinator health exposes typed Air availability"`) — verified green incl. on real Windows.
- Gates this run (standalone exit codes): gofmt 0; `go vet` 0; `go test -run Air` store **26/0**, cmd **23/0**; `swift test --filter Air` **8/8**; win testbin xcompile 0 + `GOOS=windows go vet` 0; real-Windows Air run **14/14**.
- BLOCKED (unchanged core): rows 3/4/5/7/8 (Air create on Mac → invite → join on Windows → membership → fanout → reuse) remain human-only — GUI-only on both clients, ssh session-0 ≠ interactive desktop session-1, no approved Air UI-automation harness. Raw-API forgery refused (bypasses "installed applications" mandate + secret policy). Reduced remaining owner step: Mac create Air+invite → Windows Join→review→confirm → verify/restart/reuse/dissolve.
- Artifacts: `TASK-260727-1msjz6_receipt-v3.md`, `TASK-260727-1msjz6_evidence.v3.tgz`. Secret scan clean (only 3 binary SHA-256 hashes; tokens as 16-hex fps).

### 0115 — TASK-260727-1msjz6 REVIEW → tester v2 verdict CONFIRMED; routed BLOCKED (evidence-backed)
- DECISION (reviewer/claude, RUN-260727-32989d): tester's PARTIAL PASS / BLOCKED is honest and accurate → routed `blocked` (not `done`: AC core membership rows 3–6/8 need real PASS observation that did not occur; not `to-dev`: no autonomous rework advances them, blocker is human-only GUI + Anomaly A relaunch, no code defect).
- VERIFIED first-hand (read-only, no code changed): row2 distinctness re-confirmed (orbit 9/actor 20 vs orbit 10/actor 21, orbit delta 9→10 = NEW Barycenter not device pairing); NO secret leak (only four 64-hex strings across all artifacts, all binary/blob SHA-256 hashes; tokens only as 16-hex fps); recon scripts read-only (SHA256 + DPAPI Unprotect only), plaintext zeroed; gates re-run green — `go vet` exit0, `go test -run Air` store+cmd = **42 PASS / 0 FAIL** (incl. fanout invariants + single-use invite invariant `ErrAirInviteUnavailable` in `air_control_test.go`), `swift test --filter Air` 8/8 exit0, win testbin xcompile exit0 (18,364,416 B matches receipt).
- ANOMALY A corroborated from masked Windows log first-hand (creds written 20:45:11Z, process started 20:44:37Z stayed in unpaired tray loop, `nodes_connected`=1). Recommend orchestrator file a follow-up bug (onboarded-but-not-connected in-process; `pulsar-win/unpaired_shell_windows.go`).
- CONFIRMED autonomous path exhausted: `PulsarProbe`/`winprobe` is a microphone/capture-only MSIX probe (only added device capability `microphone`), NOT an Air UI harness; ssh session-0 cannot reach interactive desktop session-1; raw-API forgery correctly refused (secret policy + no-forced-fit).
- Artifact: `TASK-260727-1msjz6_review-verdict.md`. Owner step + alternative (approved Air UI harness) unchanged from 0110.

### 0110 — TASK-260727-1msjz6 resume → two distinct Barycenters PROVEN (row 2 PASS); Air-runtime rows still BLOCKED
- CONTEXT: resume RUN-260727-3a8672 after Ivan onboarded Windows ("Create a new Barycenter") in the interactive console session. Owner ask: verify identity, no device pairing, don't assume any downstream Air row.
- MILESTONE (row 2 PASS, first-hand both machines): Mac orbit_id=9/actor_id=20/slot a/primary (Keychain bundle v2); Windows orbit_id=10/actor_id=21/slot a/primary (DPAPI `credentials.v1.dpapi` 662B, bundle v1). Distinct orbit/actor/recovery(rec_6e2f…0098b1e vs rec_6fcd…55eadc)/all-4-token-fingerprints/credential-stores. NO device pairing: coordinator `orbits` went 9→10 → a NEW Barycenter was minted (device pairing keeps the same orbit + adds a slot/node). No credential/recovery-file sharing (nothing copied between machines).
- CORRECTION to 0040: v1's "Windows never onboarded" was a store-location error — v1 checked the MSIX AppContainer LocalState, but the RUNNING/onboarded candidate is the standalone airfix binary `C:\Users\admin\AppData\Local\Programs\Pulsar\pulsar-win-amd64.exe` (sha256 `9D544F59…`, build `v0.3.0-beta.32-airfix.20260727`), which stores identity under `%APPDATA%\Pulsar`. Also: the Windows console is NOT dead — session 1 (admin) is active and the app is running (PID 11572); v1's "GUI doesn't survive" was specific to ssh session-0 activation.
- METHOD (secret-safe): Windows public identity read by in-memory `ProtectedData.Unprotect` (CurrentUser; the store uses CryptProtectData with CRYPTPROTECT_UI_FORBIDDEN only, NO optional entropy, NO LOCALMACHINE → any admin logon session decrypts) → strip 9-byte `BCDP` envelope → parse JSON → print PUBLIC fields only, tokens as sha256 fp, plaintext zeroed. Symmetric to the macOS Keychain read. Secret scan of all artifacts: clean.
- ANOMALY A (finding, worth app-team confirm): Windows onboarding wrote `credentials.v1.dpapi` at 20:45:11Z but the running process stayed in the unpaired-shell tray loop and never became a connected coordinator node — `nodes_connected` stayed 1, no `unpaired shell stopped`, no paired `pulsar-win running orbit=…` line. Per `pulsar-win/unpaired_shell_windows.go` the in-shell transition fires only when onboarding sets `didPair.Store(true)`+`requestTrayLoopExit()`; it did not. Rhymes with BUG-260727-1hjfxi (marked done). Deterministic remedy: relaunch Windows Pulsar → paired path connects as orbit 10.
- ROWS 3–8 BLOCKED: Air create (Mac GUI) + review/confirm/join (Windows GUI) can't be driven from non-interactive ssh (session-0 can't reach console session-1), no approved Air UI harness, and faking via raw prod API with extracted control tokens is refused (secret policy + no-forced-fit). Row 7 blocked = media/2nd-online-member prereq (AC-permitted). No Air currently exists (Mac log shows no recent Air activity).
- GATES GREEN this run (standalone exit codes): gofmt exit0, `go vet` exit0, coordinator `go test -run Air` store exit0 + cmd exit0, `swift test --filter Air` exit0 (8 tests/3 suites), win test binary cross-compile exit0. No product code changed; rows 10–11 (write tests/80% cov) intentionally unchecked (synthetic two-orbit tests would legitimize a forced fit). Blockers TASK-260727-1f2cyl + BUG-260727-1hjfxi both done. Artifacts: `TASK-260727-1msjz6_receipt.md` (v2), `TASK-260727-1msjz6_evidence.tgz` (v2).
- OWNER STEP (single): relaunch Windows Pulsar → confirm coordinator `nodes_connected=2` → Mac create Air + member invite → Windows Join/review/confirm → verify exactly-2-member membership + active/saved both sides → restart both → invite-reuse must fail → dissolve.

### 0040 — TASK-260727-1msjz6 two-Barycenter Mac→Win Air E2E → BLOCKED on Windows GUI onboarding/join
- VERDICT: acceptance journey BLOCKED (honest). Root cause: the Windows candidate has NEVER been onboarded (no Barycenter identity) and the only access to `win` is a NON-INTERACTIVE OpenSSH session in which the packaged GUI app does not spawn a surviving/interactive process. Onboarding + Air review/confirm/join are GUI-only; `--pair` is device-pairing (forbidden). No approved Air UI harness exists.
- EVIDENCE (Windows unpaired): `credentials.v1.dpapi` ABSENT; `%LOCALAPPDATA%\Packages\<fam>\LocalState` EMPTY; no Credential Manager / WinRT PasswordVault entry; `Roaming\Pulsar\pulsar.log` shows only `"unpaired shell startup"` (2026-07-21, 2026-07-27 21:45). FIRST-HAND launch-survival: activated AUMID `…_q036g2bzd7ngc!Pulsar` over ssh → `pulsar-win-amd64` procs=0 across 10 samples/20s, FINAL=0, no new log line, no WER event.
- FINDING (DPAPI): Windows identity store is DPAPI CryptProtectData, CRYPTPROTECT_UI_FORBIDDEN, NO LOCALMACHINE → current-user + AppContainer scoped. Even a coordinator-minted orbit cannot be injected as a valid `credentials.v1.dpapi` from an ssh context (wrong AppContainer) → no legitimate headless provisioning. Not attempted (would be a forced fit).
- CANDIDATES: Mac /Applications/Pulsar.app v0.3.0(958.1), NodeApp sha256 `4e8c986e…51d0`, Dev-ID 262RZ595FP; distinct onboarded Barycenter orbit_id=9/slot=a/actor_id=20/primary (tokens Keychain-only, fingerprinted not printed). Windows MSIX 0.3.32.0, pulsar-win-amd64.exe sha256 `99D5C034…`, go-librespot.exe `524DC89F…`. Coordinator `barycenter.relux.works/healthz`: air_rooms_enabled=true / airs_authoritative / orbits=9 / nodes_connected=1.
- DECISION: did NOT mutate live production (no Air/invite created on the owner's real Barycenter) — the journey cannot complete without a Windows peer, so a create+invite fragment adds prod churn for no PASS. Journey invariants instead verified green by the existing automated Air suite.
- GATES: `go build ./...` exit0; gofmt clean; `go vet` exit0; coordinator `go test -run Air` store exit0 + cmd exit0 (19 Air tests PASS/0 FAIL incl. exact-members/no-transitive `TestAirRuntimeResolutionUsesOnlyCurrentMembersAndStableSnapshotKey`, lifecycle+one-active, single-winner concurrency, invite redaction). Mac `swift test --filter Air` exit0 (8/8). Windows client test binary cross-compiles exit0; host run couldn't complete (transient Tailscale ssh drop to 100.120.84.42) — recoverable.
- OWNER STEP (single): at the physical Windows desktop (interactive session, not ssh) launch Pulsar → onboard "Create a new Barycenter" (NOT device invite); then Mac creates an Air + member invite; Windows Join Air → review → confirm; verify 2-member membership + active/saved on both, restart, invite-reuse fails, dissolve. Artifacts: `TASK-260727-1msjz6_receipt.md`, `TASK-260727-1msjz6_evidence.tgz`.

### 0015 — TASK-260727-1f2cyl reviewed (cycle 2) → ACCEPTED (done)
- DECISION: verdict ACCEPTED. Cycle-1 BLOCK (human-only prod gate) resolved by Ivan's explicit authorization ("давай", option A); live flip executed and now independently verified by review.
- EVIDENCE: reviewer hit public `https://barycenter.relux.works/healthz` directly (twice, stable) — `phase2.air_rooms_enabled:true` + `air_authority_state:airs_authoritative` + `status:ok` + `orbits:9`. Terminal AC (deployed endpoint, not shadow) satisfied. All other AC gates green per `rollout-receipt-executed.md` + `rehearsal-realdata.log` (create+invite probe parked/open no revision_conflict on real-data copy, orbits_readable=9, integrity/migration matches=0, backup sha256 `7aa31d60…` identical, rollback command+trigger documented).
- FINDING: re-ran gates — `go build ./...` exit0, gofmt/vet clean, 4 air-authority tests green 3/3 deterministic, store tests PASS. main.go diff cleanly scoped to air-authority additions.
- ANOMALY (updated): full `cmd/duet-coordinator` package failures are FLAKY + pre-existing across a WIDER set than just moderation — History/Overlay/Moderation, non-deterministic subset per run, incl. an occasional panic in `TestModerationHTTPRevokedAndLeastPrivilegeOperatorsFailClosed`. Proven pre-existing: same flaky set fails on a clean `git worktree` at HEAD (zero working-tree changes). None touch air-authority. CI/reviewers: package-level flakiness here is baseline noise, not Air regressions.
- NOTE (follow-ups, non-blocking): (1) `--air-cutover`/`--air-rollback` mechanism still uncommitted + not in deployed image `acb1469`; rollback depends on host-staged binary at `relux:~/aircutover-1f2cyl/` — commit/merge + rebuild image to make rollback reproducible from source. (2) stale `/healthz` version string. (3) no litestream sidecar → in-volume DB copy is sole backup. Verdict artifact: `TASK-260727-1f2cyl_review-verdict-accepted.md`. Unblocks TASK-260727-1msjz6.

### 0010 — TASK-260727-1f2cyl Air authority ENABLED on production (live cutover)
- MILESTONE: production coordinator flipped `airs_shadow → airs_authoritative`. Public `https://barycenter.relux.works/healthz` now reports `phase2.air_rooms_enabled:true` + `air_authority_state:airs_authoritative`, `status:ok`, `orbits:9`. Human-authorized (Ivan "давай", option A).
- FINDING: prod is NOT systemd (the runbook's assumption). Real deploy = Coolify app `barycenter` (id 10) as a docker container inside Colima VM `coolify` on host `relux` (198.206.134.106, macminivault Mac mini). Image `h5pm6j2dmmj8f80ffuzayuks:acb1469…` (git `acb1469`), volume `h5pm6j2dmmj8f80ffuzayuks-barycenter-data`→`/var/lib/duet`, duet uid=100/gid=101, RestartPolicy=`unless-stopped`. No litestream sidecar running (compose defines it; only coordinator deployed) → continuous S3 backup ABSENT; explicit DB copy is the only backup.
- ANOMALY: `/healthz` `version` = `git-3565c1e…` is STALE/misleading — commit `3565c1e` (2026-07-14) has NO Air code, yet the binary serves Air fields. Real deployed commit is `acb1469` (from the container image tag), ~11 commits behind local HEAD. Do not trust the reported version string; use the image tag. VERSION build-arg not refreshed per deploy.
- DECISION: cutover mechanism (`--air-cutover`) is uncommitted working-tree code, absent from deployed `acb1469`. Delivered it as a one-off: cross-compiled linux/amd64 binary run via a one-off container from the SAME coordinator image (as duet uid, coordinator stopped) against the live volume — zero image rebuild, correct file ownership. Store schema identical (`git diff acb1469..HEAD -- internal/store` empty; uncommitted `air.go` delta = `ErrAirRoomsDisabled` only, non-schema) → zero migration risk; DB stays readable by deployed `acb1469`.
- EVIDENCE: rehearsed FULL cycle on a read-only copy of the REAL prod DB (9 orbits) first — PRE airs_shadow/orbits9, cutover→authoritative, ephemeral `/healthz` orbits9+authoritative+`server_log_errors=0`, create+invite probe (parked air + open invite, no revision_conflict), clean rollback→links, held rollback on diverged (exit1), backup-restore→shadow; `orbits_readable=9` every step. Then live: stop(exit0)→backup(sha256-identical `7aa31d60…`, db+wal+shm, duet-owned, `duet.db.TASK-260727-1f2cyl-pre-air-cutover`)→cutover(result=ok exit0)→start(exit0). Post-restart logs: sessions restored, "orbits warmed up count=9 active_airs=1", error/integrity/migration matches=0.
- DECISION: live prod kept at `divergence=0` (NO live probe — probe proven on the real-data copy instead), so the clean `--air-rollback` path stays available on prod; the create+invite probe would raise divergence>0 and force backup-restore rollback.
- STATUS: KEEP verdict, no rollback. Rollback anchor = in-volume `duet.db.TASK-260727-1f2cyl-pre-air-cutover`; tooling staged at `relux:~/aircutover-1f2cyl/`. Rollback runbook (Coolify/Colima) in `TASK-260727-1f2cyl_rollout-receipt.md`. Unblocks TASK-260727-1msjz6 (Mac↔Win E2E).

## 2026-07-27

### 2352 — TASK-260727-1f2cyl reviewed → BLOCKED (human-only prod gate)
- DECISION: verdict BLOCKED, not done/to-dev. Work is accept-ready; the only gap is the real-production flip, which is outward-facing/hard-to-reverse + human-authorization gated, against a remote Coolify host unreachable from this env. Prod is still `airs_shadow`; marking done would falsely assert prod is live and mislead downstream TASK-260727-1msjz6 (manual human acceptance).
- FINDING: independently re-verified — `go build ./...` exit 0; gofmt/vet clean; 4 new `air_authority_command_test.go` tests green; `go test ./internal/store/` PASS. Rehearsal log is real (shipped binary, live `/healthz` `air_rooms_enabled:true`+`airs_authoritative`, sha256-matched backup, held rollback exit 1, restore→shadow, `orbits_readable=2` throughout).
- ANOMALY (stronger confirmation): the 2 failing cmd Moderation HTTP tests reproduce IDENTICALLY on a clean `git worktree` at HEAD (zero working-tree changes) — definitively pre-existing, in `moderation_http_test.go`, untouched by Air work. Future reviewers/CI: do not attribute to in-flight Air changes.
- NOTE: parent STORY AC says migration/rollback "rehearsed against production-shaped data" — that bar is met, so the likely close is a human re-scope+accept (option B), with the real prod flip carried into TASK-260727-1msjz6. Awaiting human decision (A run runbook now / B re-scope). Verdict artifact: `TASK-260727-1f2cyl_review-verdict.md`.

### 2340 — TASK-260727-1f2cyl Air-authority rollout: operator cutover/rollback commands
- FINDING: Air authority is a DB state machine (`air_authority` singleton: `links_authoritative → airs_shadow → airs_authoritative`, + `rollback_hold`), driven by `store.CutoverLinksToAirs`/`RollbackAirsToLinks` (generation CAS, WAL checkpoint before each authority flip, divergence + rollback-unsafe gates). Shadow is entered automatically on `Open` once active legacy links backfill (`air_schema.go:300`).
- ROOT GAP: no operator surface wired those store fns to the shipped binary — no way to enable Air authority on a deployed coordinator. Grep of non-test callers = empty.
- FIX: added one-shot operator commands `--air-cutover` / `--air-rollback` mirroring `--project-identity-rollback` single-writer pattern (`coordinator/cmd/duet-coordinator/main.go`: `runAirAuthorityCommand`, `applyAirAuthorityTransition`). Redacted before/after receipt (mode/generation/divergence/`air_rooms_enabled`). Rollback that would drop diverged Air state HOLDS (`rollback_hold`) and exits non-zero → restore-from-backup.
- DECISION: authority stays a DB-CAS operation, NOT a `coordinator.yml` boolean — a boot-time flag flipping DB authority would lose CAS/divergence safety and risk restart loops. Explicit stopped-process command matches the SQLite single-writer + systemd model.
- FINDING: once any Air is created/mutated post-cutover, `divergence_count>0` and `--air-rollback` intentionally holds; clean revert then = restore the pre-cutover backup. Pre-cutover copy is therefore mandatory.
- EVIDENCE: 4 new tests green (`air_authority_command_test.go`); `go vet`/`gofmt`/`go build ./...` clean; `go test ./internal/store/` exit 0. End-to-end rehearsal against a production-like store (shipped binary + live `/healthz`): shadow→authoritative (`air_rooms_enabled:true`, status ok), create+invite probe (parked air + open invite, no revision-conflict), clean rollback→links, held rollback on diverged store (exit 1), backup-restore→shadow. Receipt + log on board.
- ANOMALY (confirmed pre-existing, NOT mine): `TestModerationHTTPAuthPrivacyEvidenceAndDecision` + `TestModerationHTTPRevokedAndLeastPrivilegeOperatorsFailClosed` fail identically with my main.go+test reverted via `git stash`. Matches BUG-260727-1hjfxi note.
- SCOPE/BOUNDARY: real production is remote Coolify (`/var/lib/duet` volume), unreachable here + human-authorization gated. This task delivers + proves the reversible mechanism and the exact production runbook/trigger; it does NOT flip real production.
- STATUS: ready for review (to-review). Real-prod cutover is a documented human/ops trigger.

### 1906 — BUG-260727-1hjfxi Air-create failure reviewed → ACCEPTED
- ROOT CAUSE: `store.requireAirsAuthoritativeTx` returned `ErrAirRevision` for any non-`airs_authoritative` mode; production runs `airs_shadow`, so every Air read/mutation surfaced as `revision_conflict` ("Air changed elsewhere"). Create never committed. `coordinator/internal/store/air.go:1055`.
- FIX: gate now returns dedicated `ErrAirRoomsDisabled` → HTTP 503 `air_rooms_not_enabled` (`air_http.go`, `onboarding.go`, `telegram_air.go`). `revision_conflict` reserved for real CAS mismatch.
- FIX: clients gate Air UI on `/healthz` `phase2.air_rooms_enabled` (mac `MacAirAppComposition.swift`, win `windows_air_composition.go` — replaces hardcoded `AirAvailable=true`); create+initial-invite unified with preserved idempotency retry keys; conflict reload split from mutation guard (`refreshAfterConflict`).
- DECISION: new Barycenter now activates immediately; recovery export is resumable/rotatable, no longer an activation gate. Disclosed, board-approved, test-covered. Prior invariant (no activate before recovery saved) intentionally relaxed.
- ANOMALY: coordinator `cmd/duet-coordinator` has PRE-EXISTING FLAKY tests — TestHistoryHTTPReportAndBlockShareCanonicalOwnerServices, TestModerationHTTPAuthPrivacyEvidenceAndDecision, TestOverlayController{Cancellation,Reconnect}. Reproduced on clean-HEAD worktree with a DIFFERENT failing subset per run; none Air-related. Do not mistake for regressions. Track separately.
- EVIDENCE: swift 360/360; pulsar-win all 4 pkgs + `GOOS=windows` cross-compile; coordinator Air store+HTTP tests deterministic pass incl new 503/shadow cases; `git diff --check` clean.
- STATUS: done. Production stays `airs_shadow`; authority cutover deferred to TASK-260727-1f2cyl (rollback-gated), hardware E2E to TASK-260727-1msjz6.

## 2026-07-13

### TASK-260712-47uve0 — Windows DPAPI onboarding credential client
- DECISION: the legacy `credentials.json` is a bounded migration source only. The versioned destination and every recovery-pending record use current-user DPAPI plus same-volume write-through temp, flush, close, `MoveFileEx`, decrypt/read-back, exact-field verification, and only then legacy deletion or network-send admission.
- DECISION: migrated node-only state derives a canonical HTTP(S) origin from the exact preserved `/ws` URL, so legacy nodes retain an origin-bound typed capability. Remote plaintext, cross-origin re-pair merges, stale recovery acknowledgement, and partial active/pending identity matches fail closed without erasing either credential generation.
- DECISION: a sent recovery candidate is monotonic and exact-scope serialized. Authenticated `403 insufficient_capability` promotes a genuinely limited control context, while probe ambiguity, storage cleanup failure, and 401→403 retain the protected candidate; promotion resets backup acknowledgement and never changes node bytes.
- DECISION: explicit clipboard copy requires a real owner HWND/dispatcher, publishes all history/cloud exclusion formats before Unicode text, and owns a lease by sequence plus exact payload. Ambiguous Win32 sentinels and transient clear failures retain and retry that exact lease; external replacement and newer leases are never cleared by an old timer.
- EVIDENCE: deterministic host tests cover DPAPI allocation cleanup, migration crash points, strict HTTP/origin/error matrices, redirect non-replay, recovery races/restarts, one-time buffer nonpersistence, export, clipboard interleavings, and adversarial formatting. macOS host race/vet/build and Windows amd64/arm64 vet/build/test compilation pass; native DPAPI/clipboard execution, installed MSIX, and Windows hardware remain explicit review gates.

### TASK-260712-2u1w16 — macOS Keychain onboarding credential client
- INDEPENDENT REVIEW (R2): returned to development. An `ever_sent=false` pending token that matches active control state is accepted as a completed promotion and deleted without an authenticated proof; valid promotion-crash convergence can only arise after the verified false-to-true send barrier.
- FINDING (R2 migration): DP/login destination JSON is compared only after permissive `JSONDecoder` decoding. An unknown protected field is silently dropped, the payloads are treated as equivalent, and the login copy is deleted; exact-schema or complete canonical-byte conflict handling is required.
- FINDING (R2 recovery/origin): a limited authenticated promotion followed by pending-delete failure restarts as non-limited when prior orbit metadata was retained. UTS46-mapped trailing root dots are rejected because ASCII root-dot stripping runs before ICU mapping instead of after it.
- EVIDENCE (R2): actual production-source probes reproduced all four schedules. Focused 53-test, full 103-test, 50 repeated recovery/export runs, strict formatting, diff check, and release build were green, demonstrating missing adversarial coverage rather than test instability. No real Keychain or pasteboard was touched.

### TASK-260712-m5264f — Self-service onboarding and capability APIs
- DECISION (R2 durable audit): recovery rotation now writes a constrained normalized detail row containing the exact old nullable and new non-secret `recovery_id` handles beside the base `recovery.rotated` event in the same immediate transaction. Failure of the credential update, base event, detail row, or commit rolls back the generation and returns no replacement material.
- DECISION (R2 limiter audit): every HTTP limiter rejection is reserved before a durable typed `security.rate_limited` insert. The repository persists only a class-domain-separated SHA-256 subject digest and either no scope or a verified actor–orbit membership; partial, fabricated, and class-incompatible scopes are rejected. Audit failure produces the internal-error envelope without `429` or `Retry-After`, while the attempt remains consumed.
- FINDING (R2 app-first alignment): the app-first reconciliation shortcut could preserve an actor role without verifying that its sole active membership matched the credential orbit. Startup now detects the mismatch independently in reconciliation and the final serving gate, rolls back ordinary repairs, quarantines the actor with a scoped audit in a separate immediate transaction, and refuses service until explicit repair.
- EVIDENCE (R2): production-path tests cover old/new and first-generation-null rotation detail, injected base/detail rollback, collision retry, all seven HTTP N+1 limiter boundaries, scoped/unscoped audit failures, invalid scope rejection, foreign-orbit and missing-membership real reopens, quarantine failure, repair/reopen, full exact-previous-head compatibility, full uncached tests, and the full race suite.
- DECISION: app-first create and device-invite join mint independent node/control credentials in one immediate SQLite writer transaction. Only create and explicit control-authenticated rotation return recovery material; SQLite stores its non-secret handle and SHA-256 hash, while invite joins begin without recovery material.
- DECISION: authenticated onboarding mutations resolve the shared `ActorContext` at middleware, then revalidate the expected actor, current control hash in constant time, slot generation, actor lifecycle, membership role, and orbit status inside the mutation transaction. Playback-only node tokens stop at the capability boundary.
- FINDING: app-first slots intentionally have `paired_by=0`; legacy reconciliation previously classified every such slot as an orphan satellite. Reconciliation now preserves the additive role only when the generation has a provisioned control credential, while unprovisioned legacy orphans retain the conservative satellite rule.
- DECISION: forwarded transport/source headers are trusted only when `trusted_proxy` is enabled and the direct peer is loopback. A configuration boolean alone does not authenticate an arbitrary remote proxy. Phase 1 rate-limit state remains bounded and process-local; every syntactically valid rejected attempt advances the rolling window.
- DECISION: invite and recovery consumes use anchored, fixed-shape lifecycle reads and exactly one submitted-secret hash comparison. Unusable lifecycle rows select a precomputed dummy digest; valid recovery rows retain exact-tuple replay while satellite recovery restores control without changing role or granting mutation authority.
- DECISION: authenticated mutation entrypoints precompute the presented bearer digest and generated material digests before acquiring the immediate writer transaction. Transactions receive only fixed digests for constant-time comparison and persistence; plaintext material remains confined to the call boundary and one-time response.
- FINDING: device-invite capacity is live-slot capacity, not historical-coordinate count. Consume safely reuses a revoked letter by retiring any stale additive owner/credential, generation-upserting the legacy slot, and creating the replacement actor binding atomically.
- FINDING: the accepted identity foundation required two narrow security deltas for this surface: node and control lookup now enforce the slot `paired_at` generation sentinel, and Telegram resolution distinguishes known revoked/left/disabled actors without granting capabilities. Telegram principal IDs are rejected before storage lookup unless strictly positive.
- EVIDENCE: deterministic independent-connection barriers signal from the second Store immediately before `db.Begin`, then prove it cannot return while writer one is held; they cover invite single-winner consume, same-tuple recovery idempotency, and both rotation/consume orders. Further production-path tests cover stale bearer/role rechecks, transaction rollback at audit boundaries, revoked-slot reuse including a feature-off stale credential, and legacy pair-to-websocket registration. Secret-hygiene tests compare generated material against captured logs without printing secret-bearing structs, response bodies, or submitted-secret hashes on failure.

### TASK-260712-2xkyot — Telegram actor migration and compatibility
- DECISION: feature-on Telegram commands resolve the shared `ActorContext`; feature-off commands retain legacy `MemberOf` behavior. The trusted in-process bot boundary supplies verified Telegram user/chat metadata, and no public Telegram-consume endpoint or second membership authority was added.
- DECISION: link consumption holds one immediate SQLite writer transaction from hashed-code lookup and current issuer validation through actor resolution, additive membership plus legacy `members` UPSERT, conditional reservation, and audit. Same-orbit and foreign-orbit conflicts leave the code unconsumed; success never changes app-installation credential ownership.
- EVIDENCE: deterministic independent-connection races prove one winner for same-code/two-user and an unconsumed loser code for two-code/same-user. The exact pinned predecessor Store mutates a linked member through transfer, slot rebind/revoke, and leave before feature-on reconciliation restores coherent additive state.
- FINDING: fresh `BootstrapLegacyOrbit` chooses the initial primary by ranging the configured users map, so a newly seeded database can assign either configured user as primary. This predates actor migration; persisted production roles are migrated exactly. Changing fresh-bootstrap ownership policy requires a separate product/legacy-bootstrap decision.
- DECISION (R1 rework): per-verified-user Telegram link attempts use a mutex-protected exact 15-minute rolling history of the newest ten attempts. Rejected attempts replace the oldest retained boundary, and a 10,000-key least-recently-used cap bounds memory.
- DECISION (R1 rework): credential preflight performs one joined lifecycle/issuer/orbit read for every syntactically valid code, substitutes a dummy digest on a miss, and performs exactly one constant-time submitted-code comparison before any conflict disclosure or mutation.
- DECISION (R1 root audit): Telegram HTTP redirects fail closed for API and file requests while preserving an injected client's transport and timeout. Returned error graphs retain only safe cause classes; raw transport URLs, form material, Telegram descriptions, and filesystem paths are never reachable through `Unwrap`.
- DECISION (R1 root audit): the bot admits only zero-context unknown actors and known deliberately-left actors into stranger onboarding. Known revoked and disabled actors are silently denied before invite consumption or orbit/member mutation; feature-off authorization still uses the exact legacy membership path.
- FINDING (R1 combined-tree audit): concurrent onboarding work owns the shared Telegram resolver classification. This task consumed its zero/known-left/known-revoked/disabled `ActorContext` shapes without editing `identity.go`; source hashes and combined-suite evidence must therefore be rechecked together during review.
- EVIDENCE (R2 transaction barrier): Telegram consume now exposes a nil-by-default checkpoint immediately before `db.Begin`. Both independent-connection conflict tests wait for writer two at that exact checkpoint before their negative assertion; a test-only preflight sentinel makes deferred acquisition or moving the credential read ahead of acquisition fail instead of blocking later on a write. The one-winner/one-loser results and unconsumed loser-code assertion are unchanged.
- DECISION (R4 durable limiter audit): a rejected Telegram link attempt now calls the shared error-returning rate-limit audit repository with the typed Telegram-consume class, the exact decimal verified-user subject, and NULL actor/orbit scope. Only the class-domain-separated SHA-256 digest persists; the legacy debugging event is no longer written.
- EVIDENCE (R4 failure mapping): injected durable-audit failure proves the already-reserved attempt remains consumed, no rate-limit sentinel or response is emitted until persistence succeeds, and the trusted bot adapter returns its ordinary generic failure without logging the raw Telegram identity or link material. The immediately following rejected attempt persists one typed audit row and maps to the normal rate-limit response.
- DECISION (R6 privacy rework): Telegram outbox overflow and sender-failure logs retain only constant operation fields plus the sanitized adapter error. Raw private-chat, user, source-message, payload, URL, token, file, and destination identifiers are neither logged nor retained in the returned error graph, including injected alternate API failures.
- EVIDENCE (R6 real-bot paths): deterministic `bot.Bot` integration tests drive durable rate-limit success and persistence failure, committed link consumption followed by failed best-effort deletion, and exact-capacity outbox saturation. Store mutation remains nonblocking and authoritative while every transport/log surface stays identifier-free.

### TASK-260712-1bpog0 — Actor schema and scoped-auth foundation
- DECISION: `members` and `slots` remain the rollback authority while `self_service_onboarding` is off. Flag-on startup reconciles both legacy surfaces into additive actors, memberships, and generation-bound installation credentials before serving actor-aware authorization.
- DECISION: installation identity is bound to the full domain-separated SHA-256 fingerprint plus an independent stored copy of `slots.token_hash`; rebind and revoke retire the credential and actor without rewriting the legacy slot token or owner.
- FINDING: old binaries cannot enforce additive orbit/actor revocation. A rollback therefore requires the idempotent fail-closed projection: journal and zero legacy quotas, revoke affected legacy slots, and burn pending invites. Quotas restore on the next flag-on reconciliation, but projected slot revocations intentionally require explicit trusted re-pair.
- DECISION: the `orbits.status` repair uses a behavior probe and transactional create/copy/drop/rename rebuild, conservatively preserving every user view and external trigger and restoring SQLite foreign-key enforcement on every exit.
- INDEPENDENT REVIEW: returned to development. Control and node hash domains can alias because provisioning accepts a node token as a control token and the resolver checks control first, violating the node-only capability boundary.
- INDEPENDENT REVIEW: provisioning does not validate the target actor lifecycle or generation and can overwrite revoked, left, or already-provisioned target credentials.
- INDEPENDENT REVIEW: an unconstrained `orbits.status` repair runs `foreign_key_check` before rollback reconciliation, so expected stale additive children from an old-binary orbit dissolution can abort startup before cleanup.
- INDEPENDENT REVIEW: the complete previous-binary rollback, cross-domain ambiguity, combined migration, and projection crash/re-enable regression matrix remains required.
- DECISION (R1–R2 rework): node and control digests are disjoint capability domains. Provisioning and pairing reject cross-domain reuse while holding the immediate writer lock; resolution counts both domains first and fails closed for zero, duplicate, or cross-domain matches. Initial provisioning is a single-winner generation operation and cannot overwrite, revive, or provision a revoked, left, stale-bound, or satellite installation.
- DECISION (R3–R7 rework): identity DDL is one immediate transaction; status rebuild preserves the pre-rebuild `sqlite_sequence` high-water mark, narrowly removes only additive children left by an old-binary dissolve, joins migration and cleanup failures, and restores/asserts `foreign_keys=ON` on success, error, and panic paths. Flag-on orbit membership, primary transfer, leave/dissolve, and legacy bootstrap decisions now serialize from their first read.
- EVIDENCE (R8 rework): rollback coverage executes the exact pinned pre-identity coordinator revision `e8bd240664a40b9cc78b974f3c34ad30712e2aa5` through its real Store API, then verifies its newly minted node tokens as node-only after current reconciliation. Projection/restoration interruption barriers prove transaction rollback and retry; unchanged and old-binary-rebound projected-slot generations have distinct re-enable behavior. CI fetches full history and runs this pinned gate explicitly.
- EVIDENCE (R8.1 rework): one tagged integration now composes two full feature-on → rollback projection → exact pinned predecessor Store mutation → feature-on reconciliation generations against the same database. Each old interval enforces disabled-orbit lookup/pair/member/invite denial and exercises the full legacy mutation surface; generation two journals changed quotas rather than reusing generation-one values.

### TASK-260712-2y74io — Windows packaged-probe lifecycle boundary
- CORRECTION (R10 F30–F32): required structural evidence and escalation have separate immediate permits; prepare-failure, suppressed-prepare, activation-log, graceful-quit, enqueue, log, and orphan post-Release successors are likewise split or self-gated. If evidence or orphan Release is admitted and blocks across confirmed shutdown, its one-shot callback may return, but escalation, orphan removal, further evidence, lifecycle settlement, UI wake, and cleanup remain suppressed. A Windows-only cross-compile failure found during development (`showMainWindow` returns `bool` and could not be passed as `func()`) was corrected by using the typed operation-result seam before the final verification matrix.
- CORRECTION (R10 F28): the abrupt gate now grants authority per actual operation, not per waiter drain. Stop, query/read, permission/helper calls, artifact write/finalize/abort, exact-owner Release, state clear, lifecycle settlement, UI publication, and evidence I/O each load their own pre-close permit immediately before the callback. An admitted callback may publish only its minimum one-shot result after confirmation; every separately dangerous successor is rejected. Deterministic barriers cover Stop→Finalize, Finalize→Release, an in-flight Release with suppressed clear/PASS/UI/evidence, query/read return, retry rejection, confirmation boundedness, and operation-ID reuse.
- CORRECTION (R10 F29): both lifecycle diagrams now separate graceful quit, suspend, session lock, permission revoke, cancelled shutdown, and confirmed `WM_ENDSESSION`. The confirmed branch shows exact-owner nonblocking Stop before latch/wake, per-operation rejection, one bounded no-sync append to the existing partial, waiter exit, and Windows/startup-recovery ownership; it contains no Finalize, Abort, Release, evidence sync, resource cleanup, or lifecycle PASS claim.
- CORRECTION (R8 W4): a real prepared publication loser now atomically claims and stores its unique Stop producer before its orphan obligation becomes waiter-visible, then invokes native Stop. A drain in the publication-to-invocation window is deterministically pending with zero query/release; confirmed shutdown remains independent and never waits for or drains the orphan.
- CORRECTION (R8 W3): every task-owned ABI/ownership structural path now uses one required-evidence continuation whose API orders `requiredEvidence` before `escalate`. A passing redacted diagnostic precedes `beginGracefulQuit`; failed/sticky evidence suppresses that successor and leaves the existing evidence-failure path authoritative; confirmed shutdown suppresses both.
- CORRECTION (R8 W1): finalized release retry no longer assumes terminal evidence. It derives authority from either the exact observed native terminal bit or the same owner's completed `S_OK` Stop. `E_ILLEGAL_METHOD_CALL` immediately after nonblocking Stop is retained as an honest waiting-for-terminal retry, while failed/unset Stop never authorizes Release or cleanup settlement.
- CORRECTION (R8 W2): same-generation duplicate prepare delivery is phase-rejected before a second helper call. Any real successful loser behind a distinct/stale active owner is published as an exact waiter-owned orphan before its one-shot Stop; ordinary cleanup queries that loser to terminal and releases/retries only its immutable owner, while recording no artifact or incumbent lifecycle settlement. Active/orphan ownership blocks new recording, `CLEANUP_READY`, and `CapDestroy`; confirmed shutdown admits no orphan query/release and leaves OS recovery authoritative.
- CORRECTION (R8 F25): Stop acceptance, immediate/deferred callback ownership, exact HRESULT publication, terminal observation, and the actual waiter-owned `CaptureRelease` invocation now linearize on the immutable `(generation, operation ID, owner pointer)`. Release cannot overtake a live Stop/activation producer; only exact `S_OK` marks that same owner released, failed release remains retryable, and operation-ID reuse cannot receive stale work.
- CORRECTION (R8 F26): `CapturePrepare` success with operation ID zero is rejected before lifecycle commit/publication. No Stop/query/Release receives zero, the generation is settled, required redacted evidence is acknowledged before bounded quit escalation is armed, and the next start gate is not stranded. A first `ERROR_INVALID_HANDLE` result query for a nonzero app-owned operation is treated as structural helper failure with no fabricated terminal/release cleanup.
- CORRECTION (R8 F27): query-failure artifact finalization and release require the exact recorded Stop result `S_OK`; failed and unexpected non-`S_OK` Stop results retain both artifact/native obligations and enter bounded fail-closed exit. `CaptureRelease` likewise transfers ownership only on exact `S_OK`; every other HRESULT retains the exact owner and suppresses lifecycle release settlement.
- CORRECTION (R3 rework): the earlier R1/R2 statement that settlement replay was comprehensive was false. Terminal, artifact-disposed, and native-release observations now persist independently in a monotonic per-generation ledger even before stop publication; ordered replay targets only runs bound to that generation, and generation N+1 cannot settle generation N.
- DECISION (R2–R6 rework): permission-ready, capture-ready, discovery, picker, and rearm continuations validate the live generation/gate before invoking native starts. Runtime `CapPermissionCheck` is waiter-owned, and graceful quit is a non-droppable intent observed independently of the bounded ordinary command queue.
- DECISION (R5/R7/R9 rework): helper lifetime and exit arbitration use portable atomic coordinators; the 30-second hard deadline remains armed until `WM_QUIT` commit and performs no blocking log/sync on its sole exit path. Tray ownership clears only after successful `NIM_DELETE`, with retry/fail-closed escalation on timer failure.
- DECISION: the hidden top-level window owns `WM_QUERYENDSESSION` / `WM_ENDSESSION`, `WM_POWERBROADCAST`, and registered `WM_WTSSESSION_CHANGE`; `AppCapability.AccessChanged` plus the existing bounded `CheckAccess` poll owns permission revoke.
- DECISION: lifecycle evidence is ordered by a production `cleanupId` state machine. It refuses to claim artifact disposal, capture release, helper destruction, or exit before the corresponding observed postcondition.
- FINDING: `WM_ENDSESSION` is an abrupt OS-owned lifetime boundary. The probe binds the exact active generation and requests only its nonblocking stop before publishing a monotonic confirmation latch and waking one best-effort waiter drain. After the latch, the wndproc returns without hotkey unregister, evidence, stage advancement, replay, or resource release; the waiter only performs bounded buffered reads into the existing no-sync partial. Windows and startup recovery own every remaining boundary. Signed-MSIX Windows 10/11 runs must measure actual delivery and file state.
- FINDING: WTS registration and actual power/session notification delivery cannot be established on the macOS host. A registration failure is therefore structured `blocked` evidence with `GetLastError` and a hardware-gate next action, not a fabricated session-lock pass.
- DECISION: cleanup retries the real `ArtifactWriter.Abort` ownership/postcondition after query/finalization failure. New recording and graceful exit remain blocked while any owned temporary path is not verified absent.
- DECISION (R3 rework): idle cleanup and permission rearm are durable acknowledged UI intents retried after `PostMessageW` failure. Rearm remains a start gate through current-generation permission validation plus discovery/hotkey work; stale results cannot mutate permission state.
- DECISION (R3 rework): `AccessChanged` query failure and runtime query failure with an owned capture generation close the permission gate and request native stop before diagnostic I/O. OS lifecycle window messages use direct nonblocking stop plus queued evidence rows.
- DECISION (R3 rework): evidence writes/syncs use one bounded serial coordinator. Any write, sync, saturation, or timeout failure is sticky and suppresses clean evidence/process-exit claims; the serialization boundary recursively removes local paths, picker filenames, usernames, tokens, and payload content.
- DECISION (R3 live review): durable UI intent dispatch has explicit pending/posting/queued states. Exact-ID consumption is valid while `PostMessageW` is in flight, and the post completion path observes that consumption without recreating or duplicating the intent.
- DECISION (R3 live review): each required permission/start evidence row is an acknowledged production continuation gate. An error or stall settles the exact generation and prevents permission, prepare, or activation work; startup evidence failure likewise prevents hotkey/discovery enablement. Privacy sanitization is bounded for cyclic values and redacts an entire value when it detects an absolute Windows, UNC, or POSIX path or credential text.
- DECISION (R3 F9.1): a failed waiter-owned explicit-record or lifecycle-rearm permission query enters permission-revoke cleanup before its diagnostic row. Requested generations are settled in the in-memory ledger before evidence I/O; failed rearm tokens are invalidated with no hotkey, discovery, permission-ready, or capture continuation.
- DECISION (R3 privacy follow-up): absolute-path detection is conservative at the serialization boundary: root-level POSIX files followed by punctuation and local `file:///` URIs trigger whole-value redaction. Free-text token and authorization credentials are also removed when separated by whitespace, not only by `=` or `:`.
- DECISION (R3 device evidence): the top-level `DeviceID` is trusted only from Windows default-device/enumeration results. Recognized MMDevice and `\\?\SWD#MMDEVAPI#...` interface identities remain observable despite their UNC-like syntax; credentials and real filesystem paths are still redacted, and recursive fields receive no exception.
- CORRECTION (R4 F11): the earlier claim that every accepted evidence row drains after a sticky failure was unsafe. The first write/sync/error/timeout/saturation failure now suppresses logger and sync invocation for all queued successors; asynchronous rows are discarded and synchronous rows receive the stable non-nil failure.
- EVIDENCE (R4 F10/F11/W1–W4): production lifecycle/stop/drain, evidence-worker, wndproc-entry, and late-callback schedules prove confirmation does not wait for a pre-confirmation permit, repeated `WM_ENDSESSION` adds no effect, and the only post-latch native/file callbacks are capped `CaptureRead` and no-sync append. Both wndprocs suppress queued app/timer/hotkey/command/close/cancel/resume messages with protocol return values. Evidence rejects before enqueue and physical callback; SIGTERM/evidence-failure, watchdog, post-pump error UI/logging, and deferred cleanup add zero post-latch effects. Blocked write/sync plus saturation schedules prove no passing cleanup row survives a missing prerequisite.
- DECISION (R5 F12): confirmed `WM_ENDSESSION` no longer enters `lifecycleTracker.mu`. An immutable atomic owner snapshot binds the current capture generation and native operation; only the matching successful release may clear it. Confirmation closes the abrupt gate, requests the exact owner's one-shot shutdown stop, latches, wakes, and returns. A prepare/activation callback already holding the lifecycle tracker may finish, but a late prepared operation stops itself and every evidence/UI/result/release/finalize successor is rejected by the same production continuation gate.
- CORRECTION (R5 F13/F14): `closing` and `confirmed` are distinct abrupt states. During stop-to-latch, ordinary waiter and wndproc callbacks are suppressed, but the waiter remains alive until confirmation makes the bounded no-sync drain claimable. Deterministic production-seam barriers now prove prepare and activation nonblocking behavior, a real pre-lock activation attempt, exact stop-before-wake ordering, zero successor callbacks, and exactly-once abrupt dispatch under race repetition.
- CORRECTION (R6 F15): every successful native prepare now creates an exact immutable owner even when atomic publication loses to an existing owner. The unpublished operation claims its own one-shot cancel stop synchronously at the helper-result seam, independently of the ordinary continuation and abrupt gate; stale disposal cannot clear or stop the distinct active owner, whose confirmed-shutdown stop remains exactly once before wake.
- CORRECTION (R6 F16): capture-owner publication acceptance and lifecycle tracker commit now share the tracker mutex. A same-generation A/B conflict restores A's phase and operation before unlock, exposes both exact immutable owner objects in the prepare result, and suppresses later settlement of A; a real lifecycle stop therefore binds/stops A rather than the cancel-stopped B.
- CORRECTION (R6 F17): post-helper prepare-result evidence and successful-owner successors have separate admission gates. An ordinary native prepare failure emits its single HRESULT row with no owner/activation; abrupt close suppresses the row, and a failed duplicate restores A while reporting only the failed attempt. Successful unpublished B still admits neither evidence nor owner successors.
- CORRECTION (R6 F18): a prepare refused before the helper now classifies exact native ownership before suppressed settlement. A queued same-generation duplicate after suspend/lock is diagnostic-only and cannot fabricate terminal/artifact/release/cleanup for bound A; a genuinely pre-native distinct invalidated generation retains the existing fail-closed settlement path.
- CORRECTION (R6 F19): published prepare completion now exposes exact app operation/generation state before the potentially blocking result-evidence callback. Immediate auto-reset readiness therefore observes the owner and may continue activation; evidence failure claims that owner's one-shot cancel stop before quit escalation, and terminal cleanup reuses the recorded HRESULT rather than duplicating native stop. Failed and unpublished prepares still publish no state.
- CORRECTION (R6 F20): queued readiness now claims activation intent on the exact owner before emitting activation-intent evidence. Owner stop, intent admission, and native admission are atomic state transitions: stop-first suppresses all activation work; intent-first may still be stopped before native admission; native-first permits the admitted callback and the exact one-shot stop follows without any later cleanup or shutdown stop duplication. Active capture stop paths reuse the same owner claim.
- CORRECTION (R6 F21): exact-owner stop reuse now returns explicit not-requested, pending, or completed state rather than representing an in-flight callback as `S_OK`. Capture query-failure cleanup retains artifact/native ownership while pending and runs abort/release only after the recorded result is visible; confirmed shutdown remains nonblocking and suppresses the later ordinary retry, while a non-confirmed terminal retry releases once.
- CORRECTION (R6 F22): native activation admission now owns the helper-call interval through an explicit completion transition. A stop that wins before admission suppresses activation; a query, hotkey, lifecycle, or confirmation stop arriving after admission is retained as pending and invoked exactly once only after the admitted `CaptureActivate` call returns. Confirmation never waits, and query-failure cleanup cannot finalize/release while either activation or its deferred stop is in flight.
- CORRECTION (R6 F23): every successful native activation admission arms its completion/abandon defer before the post-admission shutdown check. If confirmation closes the gate at that exact boundary, no `CaptureActivate` call occurs, the retained stop runs once through the abandon completion seam, and pending cleanup remains unable to finalize or release.
- CORRECTION (R6 F24): lifecycle and active-capture stop callbacks now preserve exact generation plus operation identity through the native-stop seam. A terminal release/owner clear turns a late callback into an explicit not-requested result; it never falls back to native `CaptureStop`, and a reused operation ID from another generation cannot be claimed.

## 2026-07-06

### 1641 — EPIC B: pulsar-win Windows shell skeleton shipped (blind build)
- MILESTONE: commit ea5321b pushed; CI run 28792158823 fully green (node-core, pulsar-win, coordinator).
- FINDING: Go internal-package rules block importing `coordinator/internal/protocol` from a sibling module even with require+replace ("use of internal package … not allowed", empirically probed).
- DECISION: `pulsar-win/wire/` = verbatim mirror of the protocol package, pinned by two tests: golden round-trip of every `protocol/golden/*.json` + `TestMirrorMatchesCoordinatorSource` (pulsar-win/wire/golden_test.go).
- NOTE: mirror compare is gofmt-normalized because `coordinator/internal/protocol/codec.go` is itself not gofmt-clean and coordinator/ belongs to the EPIC A agent — do not "fix" it from pulsar-win.
- NOTE: CROSS-AGENT TRIPWIRE — any coordinator protocol change now fails the `pulsar-win` CI job on main until wire/ is re-copied (cat mirror header + source, `gofmt -w`). Intended keep-in-sync mechanism, not a flake.
- FINDING: reproducible Windows build verified bit-identical locally — sha256 `c75fa3c0…` twice with `CGO_ENABLED=0 -trimpath -ldflags "-s -w -buildid="`.
- FINDING: go-wca v0.3.0 exports `AUDCLNT_STREAMFLAGS_AUTOCONVERTPCM`, so the WASAPI render loop runs at pipeline-native 44100/2/f32 with no local resampler (pulsar-win/audio_windows.go).
- STATUS: portable parts unit-tested (+race); WASAPI/named-pipe/AppContainer legs untestable until a Windows machine (U-W1..U-W5); fork's Windows pipe output driver = P1, not built yet.

## 2026-07-20

### TASK-260712-1ulshp — External E2EE implementation-security review: ACCEPTED (disabled-framework scope)
- DECISION: independent implementation-security sign-off of the DISABLED (production-dark) E2EE framework at frozen candidate 9d7ace6 (tree ef819c9). No open Critical/High. NOT production crypto approval; e2ee_media stays OFF; no provider/suite/SBOM/manual/hardware/storage-capture/moderation-workflow/rollout/beta claim. Activation still gated by TASK-260712-yj668d/30xwu2/1actom (all remain in backlog, untouched, under the manual acceptance epic).
- CONSTRAINT (non-obvious, environmental): the task brief names user-approved reviewer = Claude Fable 5 max, but Fable 5 is CREDIT-EXHAUSTED in this environment. Prior run RUN-260720-4cb4ad (Fable 5) produced no verdict on the same limit; a re-attempt this run to delegate to 5 parallel Fable 5 dimension agents ALSO hard-failed ("You've reached your Fable 5 limit"). Per the board-recorded recovery decision, the review was completed under claude-opus-4-8 (RUN-260720-191344) as the next available independent NON-IMPLEMENTING reviewer. If cross-model Fable-5 independence is a hard gate, a human can replenish Fable 5 and re-run at zero operational risk since nothing ships.
- EVIDENCE: boundary clean (9d7ace6..909e739 = tooling/packet/board/planning only; no product/runtime or dependency-manifest delta); working-tree product == 9d7ace6. Reproduction green: generator --check, both validators, 9/9 packet+parity unit tests, coordinator -race (e2eecontract/store/moderation). Direct adversarial code read (not packet-trust) across 8 dimensions confirmed: ciphertext-only coordinator (contract.go forbidden-fields + DisallowUnknownFields; e2ee_schema.go has no key/plaintext cols), fail-closed downgrade/suite/signature (empty AllowedSuites + nil Verifier in ProductionConfig), strict commit-chain/epoch-equality/replay/nonce guards (contract.go AcceptContent/ApplyCommit; opaque_live frame binding), current-epoch-bound single-use TTL grants+transfer (e2ee_recovery.go), verified/non-revoked device auth, metadata-only reports + separate consent-gated ciphertext evidence + append-only audit (e2ee_report_moderation.go), device-only Keychain/DPAPI zeroizing key leases (MacE2EEKeyState.swift / windows_e2ee_key_state.go).
- RESIDUAL (Info/Low only): KDF domain-separation labels + AEAD suite provider-deferred (E2EE-PACK-R03); forbidden-field scan is top-level-only but backstopped by DisallowUnknownFields; pre-existing gofmt on out-of-interval phase-1 file. R01-R05 carried with owners.
