# Self-contained Pulsar Audio — specification entry point

The user-provided goal explicitly designates
[`docs/spec-self-contained-audio.md`](../docs/spec-self-contained-audio.md) as
the source of truth. Read that document in full before planning or executing
any task under this goal.

The concise execution contract is
[`docs/goal-self-contained-audio.md`](../docs/goal-self-contained-audio.md).
Current shipped behavior and compatibility constraints remain documented in
[`docs/spec.md`](../docs/spec.md) and
[`docs/protocol.md`](../docs/protocol.md).

This entry point exists for the repository's `.spec/` workflow. It deliberately
does not duplicate the authoritative specification: requirements, numbered
acceptance scenarios A1–A8/B1–B7/C1–C7, phase gates, and invariants must be
traced to the linked source document without narrowing or reinterpretation.

