# Non-secret coordinator rollout switches for the native systemd deployment.
# Keep current-only fields out of coordinator.yml while the pinned predecessor
# remains a rollback target. Empty/off = disabled; 1 = enabled.
DUET_SELF_SERVICE_ONBOARDING=
