# Prove C4-C6 and publish the code-review packet

## Description
Run cryptographic interoperability privacy key-loss and report evidence before the final external implementation review.

## Scope
Execute known-answer, malformed, fuzz and state-model suites plus real Windows to Windows, Windows to macOS and macOS to macOS clips, tracks, saved cues and live PTT. Cover concurrent join or leave, removed and compromised devices, coordinator storage and traffic capture, replay, nonce uniqueness, rollback or clone, recovery success and irreversible loss, explicit history scope, mixed version and downgrade, metadata disclosure, report consent and evidence expiry. Hash builds and artifacts and publish threat model, ADR, reviewed design, SBOM, protocol, attack scripts, residual risks and exact feature-flag claims for the independent implementation review owned by acceptance.

## Acceptance Criteria
C4-C6 pass for every claimed path; coordinator artifacts cannot reproduce protected content, removed members cannot decrypt new media, new devices cannot read ungranted history and report evidence exists only after consent. All critical and high local findings are fixed, unsupported paths are named, live_ptt and e2ee_media claims remain separate and the final external reviewer can reproduce the packet. No external review is self-certified by this task.
