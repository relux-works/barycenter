# Implementation guard — TASK-260712-m5264f

Before editing, read the complete task card and both attached diagrams; `docs/spec-self-contained-audio.md` sections 3.13, 6, 11, 12, 18, and 19; the complete accepted Rev15 contract at `.task-board/.resources/TASK-260712-3v1k7q/research.md`; `.task-board/.resources/TASK-260712-3v1k7q/p1-root-review-amendments.md`; and the accepted identity implementation and review evidence for `TASK-260712-1bpog0`. The accepted contract and source specification are authoritative where a diagram is simplified.

Implement only the Phase 1 coordinator onboarding/capability surface in this task. Reuse the shared `ActorContext`, domain-separated credential lookup, generation-bound installation identity, immediate writer transactions, rollback projection, and feature flag from the accepted identity foundation. Do not introduce a second auth resolver, alternate schema, public Telegram-consume HTTP route, client secure-storage behavior, or Phase 2 behavior. Preserve the dirty worktree; do not commit or push.

Every create, invite consume, recovery consume/rotate, actor-context probe, and Telegram-link issue operation must follow the exact accepted request/response/error and lifecycle contract. Authorize with capability × role; a node token is playback-only and must never provision or recover control authority. Validate the actor, membership, orbit, unrevoked slot, binding generation, and credential lifecycle inside the same `BEGIN IMMEDIATE` transaction as each mutation. Creation and consume are single-winner and auditable. Recovery uses the accepted generation predicate and retry/idempotency protocol; it returns replacement material only where specified and never rewrites the node credential. Legacy pair/websocket behavior and feature-off service must remain compatible.

Generate secrets with the frozen entropy/alphabet rules. Persist only the accepted hashes; no plaintext node, control, recovery, invite, or link secret may enter SQLite, URLs, logs, fixtures, snapshots, errors, or outcome artifacts. Apply the frozen bounded syntax checks, source/IP and per-identifier attempt ordering, uniform non-oracular errors, `Retry-After`, constant-time/dummy-hash behavior, expiry, invalidation, and atomic one-time consume semantics. Do not claim distributed rate-limit durability beyond the accepted single-process contract.

Add production-path tests for the full capability × role matrix; node-token negative administration/upload cases; create/join/recover/rotate/probe success and exact errors; hash-only persistence and response redaction; expired, invalidated, replayed, brute-force, cross-orbit, revoked/left/stale-binding targets; concurrent consume/rotate/provision races using independent database connections and deterministic barriers; legacy pair plus websocket registration; flag-off behavior; migration/rollback coexistence; and audit atomicity. Run focused repetitions, the full uncached coordinator suite, race, vet, build, formatting, diff, and board validation. Do not count skips, SQL emulation of public behavior, or external CI as local evidence.

Attach a new task-scoped outcome with exact file SHA-256 hashes, endpoint/AC/test mapping, unabridged commands and pass/fail results (including corrected failures), dirty-worktree scope, and honest external boundaries. Leave the task in `to-review`. It requires a fresh independent security/protocol/migration review and a new root line-by-line/hash/test audit before any downstream task may treat it as accepted.
