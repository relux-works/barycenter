# STORY-260721-2p02ge: consolidated-owner-verification

## Description
One final human-owned verification gate executed only after autonomous engineering and deterministic acceptance are complete. It replaces the nineteen fragmented manual tasks and gives Ivan Oparin one exact-build checklist covering the highest-value real-app, hardware, accessibility, rollout and recovery risks.

## Scope
One Windows pass on mbpro-win plus one macOS pass on the approved local Mac, using ordinary installed application launches and built-in microphones unless the checklist explicitly calls for another route. Include the smallest high-signal end-to-end matrix for permissions, capture/playback, overlays, UI, real transport, recovery, cleanup and required soak. Do not require the owner to interact during engineering.

## Acceptance Criteria
Exactly one child task is assigned to Ivan Oparin. Its handoff identifies exact signed/notarized build hashes, installation steps, a concise ordered checklist, expected observations and one artifact bundle location. Legacy manual tasks are closed as superseded without being marked passed. Any failure reopens a focused engineering defect; the task is done only after an honest owner verdict packet.
