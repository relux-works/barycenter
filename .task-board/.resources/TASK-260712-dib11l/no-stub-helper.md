A stub or placeholder DLL is NOT an acceptable deliverable. This task must add the actual native C++/WinRT helper implementing the accepted ABI paths needed by P1: version/init/destroy, AppCapability permission request/check, device enumeration/default selection, ActivateAudioInterfaceAsync plus event-driven WASAPI capture/read/stop/release, and FileOpenPicker plus take-once readable HANDLE. Runtime fail/blocked evidence is valid only after the real API path executes and reports a platform HRESULT; missing/unimplemented behavior must not be relabeled platform evidence. Add native build/test wiring and package the helper DLL. If you cannot complete the actual helper, leave DoD unchecked and report the implementation incomplete; do not present portable controller or loader scaffolding as task completion.