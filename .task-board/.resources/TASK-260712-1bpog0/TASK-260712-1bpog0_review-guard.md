# Independent review guard — TASK-260712-1bpog0

Review only; do not implement fixes. Read the task, producer outcome, every changed file in full, docs/spec-self-contained-audio.md sections 3.13, 6, 11, 12, 18, 19 P1.2, the complete accepted Rev15 recovery/link contract in TASK-260712-3v1k7q/research.md, root amendments, and the identity model. Do not trust producer summaries or green tests without reproducing them.

Scrutinize every schema constraint, migration branch, SQLite transaction/locking assumption, legacy backfill/reconciliation, feature-off and previous-binary behavior, rollback projection, orbit dissolve/disable/re-enable, slot rebind/revoke, and authorization resolver branch. Prove roles, paired_by, live legacy node tokens, quotas, settings, invites, and tenant isolation cannot change accidentally. Check node/control/Telegram capability separation; constant-time and canonical hash lookup; no plaintext secret persistence/logging; collision, replay, stale-generation, disabled/left/revoked paths; ambiguous matches; error leakage; and fail-open/fail-closed choices. Look for SQL injection, integer/time/null edge cases, broken FKs, deadlocks, partial migration state, irreversible rollback surprises, or schema object loss.

Independently run full uncached tests, store race tests, vet/build, targeted migration/downgrade/feature-off/authorization tests, and inspect representative databases directly where useful. Report every finding with severity, exact file/line, concrete failure schedule/data, violated AC/invariant, and required correction. Attach a task-scoped review outcome. Approve only if no substantive finding remains; otherwise return to rework. Never mark done: root line-by-line review remains mandatory.