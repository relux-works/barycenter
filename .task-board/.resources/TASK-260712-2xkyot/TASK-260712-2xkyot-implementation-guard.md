# Implementation guard — TASK-260712-2xkyot

Before editing, read the complete task card and both attached diagrams; `docs/spec-self-contained-audio.md` sections 3.13, 6, 11, 12, 18, and 19; the complete accepted Rev15 contract at `.task-board/.resources/TASK-260712-3v1k7q/research.md`, especially sections 10–12 and 17.7–17.8; `.task-board/.resources/TASK-260712-3v1k7q/p1-root-review-amendments.md`; and the accepted identity implementation and review evidence for `TASK-260712-1bpog0`. Treat the accepted contract and source specification as authoritative.

Implement only the Telegram adapter migration, trusted in-process link consume, and legacy/self-service coexistence owned by this task. Reuse the shared `ActorContext` and identity repositories; do not add a bot-specific authorization model, public Telegram-consume endpoint, second membership authority, schema replacement, or Phase 2 behavior. Preserve the dirty worktree; do not commit or push.

Route bot authorization through capability × role `ActorContext` without changing existing orbit roles, primary transfer, leave/dissolve, revoke, pair, share, or playback semantics. Telegram metadata and update payloads are untrusted input; the consume method is callable only from the authenticated/trusted adapter boundary frozen in the contract. Linking a Telegram actor to an app-owned orbit must not transfer app-installation ownership or turn Telegram into the credential owner. Apply the exact desired-role, issuer-role, same-orbit idempotency, foreign-orbit conflict, revoked/left/disabled actor, expiry/invalidation, and uniform-error rules.

The link consume path must be one `BEGIN IMMEDIATE` transaction from code reservation through conflict checks, actor resolution, additive membership/legacy dual-write, and audit. Conditional single-use consume has exactly one winner. Use conflict-safe UPSERT behavior; never `INSERT OR REPLACE`. Keep additive rows and legacy `members` coherent in both directions according to the accepted rollback/reconciliation policy. Old pair/node tokens stay node-only and valid where the accepted identity foundation says they do; no node token gains link issuance or administration authority. Plaintext link codes and credentials must not enter storage, logs, fixtures, snapshots, errors, or outcome artifacts.

Add production-path bot/store tests for migrated existing primary/companion/satellite members; mixed legacy and self-service pair flows; shared `ActorContext` command authorization; successful, idempotent, expired, invalidated, replayed, cross-orbit, same-code/two-user, two-code/same-user, and concurrent consume cases with deterministic barriers and independent database connections; role preservation and transfer/leave/revoke behavior; transactional rollback on legacy dual-write/audit failure; feature-off and exact previous-version reconciliation where relevant; and secret/log redaction. Run focused repetitions, full uncached bot/store/coordinator suites, race, vet, build, formatting, diff, and board validation. Do not use SQL-only emulation as proof of adapter behavior or claim unrun Telegram/external evidence.

Attach a new task-scoped outcome with exact file SHA-256 hashes, AC/flow/test mapping, unabridged commands and pass/fail results (including corrected failures), dirty-worktree scope, and honest external boundaries. Leave the task in `to-review`. It requires a fresh independent security/compatibility review and a new root line-by-line/hash/test audit before downstream work may rely on it.
